#include<stdio.h>

int main(){
    // int i = 34;
    // int *ptr = &i;
   // printf("The value of ptr is %u\n", ptr);  // integer is of 4 bytes
    // // ptr = ptr - 1;
    // ptr = ptr + 2; 2x4
   // ptr++;
    // // ptr++;
   // printf("The value of ptr is %u\n", ptr);

    // char c = '3';
     //char *ptr = &c;
     // printf("The value of ptr is %u\n", ptr);  // chatater is of 1 bytes
    // // ptr = ptr - 1;
    // ptr = ptr + 1;
    // ptr = ptr + 1;
    // ptr++;
    // // ptr++;
    // printf("The value of ptr is %u\n", ptr);

   /* float f = 3.4;
    float *ptr = &f;
    printf("The value of ptr is %u\n", ptr); // 4 bytes
    // ptr = ptr - 1;
    ptr = ptr + 1;
    // ptr = ptr + 1;
    // ptr++;
    // ptr++;
    printf("The value of ptr is %u\n", ptr); */


    return 0;
}